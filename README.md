# weather-api-project
This Python program uses the requests library to fetch real-time hourly weather data from a public API. It parses the JSON response, extracts time and temperature values, loops through them, and applies conditional logic to display only hours where the temperature exceeds 20°C in a readable format.
